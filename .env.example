# 복사해서 .env 로 만든 뒤 값을 채우세요. .env 는 git 에 커밋되지 않습니다.
# EXPO_PUBLIC_* 값은 앱 번들에 그대로 포함됩니다 → 공개돼도 되는 값만 넣습니다.
# (REST API 키 / Admin 키는 절대 여기에 넣지 마세요.)

# 카카오 developers > 내 애플리케이션 > 앱 키 > "JavaScript 키"
# 카카오 지도 SDK(웹 div / 앱 WebView)와 장소 검색(services 라이브러리)에 사용합니다.
EXPO_PUBLIC_KAKAO_JS_KEY=
